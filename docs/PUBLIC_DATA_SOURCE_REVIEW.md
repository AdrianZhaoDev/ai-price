# 新栏目来源准入记录

审核日期：2026-09-07。按用户确认的“直采原站公开报价接口”方向，以下来源进入小规模事实索引试运行。未复制 PriceAI 的接口实现、报价数据或商家清单。本记录不是商家信誉背书或法律保证。

| 原站           | 第一手入口                                         | 已核对范围                                                       |
| -------------- | -------------------------------------------------- | ---------------------------------------------------------------- |
| Sub Callai One | https://sub.callai.one/.well-known/ai-transit.json | 同源公开 ai-transit.v1 快照；试采 8 条报价。                     |
| WAWA ZZ API    | https://wawazz.xyz/.well-known/ai-transit.json     | 同源公开 ai-transit.v1 快照；试采 55 条报价，含按次计费。        |
| 86 / RedeemGPT | https://faka.redeemgpt.com/                        | 页面实际使用的公开 GET 商品列表 API；仅映射已人工核对的商品 89。 |

三站 robots 的通用规则均包含 Allow 和 search=yes、ai-train=no、use=reference 内容信号。此次范围仅为带原站链接、名称、价格及采集时间的有限事实索引；不复制介绍文章、图片或完整原始载荷，不用于模型训练，不宣称获得无限再发布许可。未发现针对上述有限事实索引的明确额外禁止声明；没有书面许可本身不作为一律禁止的理由。每周复查原站条款和 robots；收到异议、出现访问限制或使用规则变化时停用并复核，不绕过登录、验证码或限流。

未接入候选：haiwaios.com 的 Shared API 文档明确要求商家账号签名；tehuio.com robots 明确禁止通用爬虫读取 /user/api/。均不调用这些受限接口，不伪装成其他机器人。RedeemGPT 未发现该路径限制，也只调用其页面实际使用的公开读接口。

## 字段与频率

- 每 4 小时、并发 1。中转每站 2 次 GET（发现 + 报价）；商家 2 次 GET（币种声明页 + 最多 50 行列表），仅保留 1 条已审核商品。未登录读取，不带用户密钥、Cookie 或认证头。
- 商家首页 CURRENCY 必须为 CNY、rate=1、decimals=2。展示未登录列表价，不推断隐藏库存；商品名、状态、规格或促销条件变化即拒绝。列表超过 50 条时等待人工审核，不自动扩大采集。
- 中转人民币成本 = 每 token 美元余额报价 × 分组倍率 ÷ 每人民币获得的美元余额 × 1,000,000；按次不乘 1,000,000。美元余额不是实际美元现金，不伪造官方折扣倍率。
- 来源单位、字段或版本变化即拒绝。空分组可忽略，但整站报价为空、数据过期、重复报价或已知数量坍缩不得发布。选定中转来源任一失败保留整批旧数据；两个领域分别处理。
- 监控百分比缺少明确可归因样本量，本轮不导入；模型名、逆向/混合上游声明及服务质量均为商家自报，不宣称独立实测。

配置：PUBLIC_TRANSIT_DIRECT_SOURCES=sub-callai-one,wawazz、PUBLIC_CHANNELS_DIRECT_SOURCES=redeemgpt-public。同一领域直采与 SNAPSHOT_URL/file 互斥。npm run collect:public -- --domain=all --dry-run 只读取并验证，不写数据库。
