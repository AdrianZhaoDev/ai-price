# 顶部栏目导航

顶部统一按以下顺序显示：全球区价、国内订阅、API 价格排行榜、卡网报价、API 中转。
英文使用同样顺序和对应 `/en` 路由。

- 卡网和中转列表不再显示独立的小切页；中转详情只保留返回目录链接。
- 当前栏目通过顶部高亮及 `aria-current="page"` 标识，详情页仍选中 API 中转。
- 宽屏在品牌与语言/主题操作之间显示五个入口；1180px 及以下移至头部第二行。
- 620px 及以下在同一个主导航内按三项、两项排列，保留可读文字与至少 44px 点击高度，不横向滚动。
- 沿用现有色彩、焦点、减少动画及主题设置，不改变报价、来源、采集任务或数据库。

回归验证覆盖中英文入口顺序、路由切换、选中状态，以及 320–1440px 布局边界。
发布沿用 VPS 标准流程；无新增迁移或环境变量，回滚代码不需要回滚数据。
